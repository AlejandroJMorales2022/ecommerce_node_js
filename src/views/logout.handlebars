<div class="uk-padding-large uk-child-width-1-1" uk-grid>
  {{#if error}}
  <h6 style="color:tomato">{{error}}</h6>
  {{else}}
  <h3>Hasta luego, {{user}}!</h3>
  {{/if}}
  <a href="/login">Ir a inicio</a>
</div>
